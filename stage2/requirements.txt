fastapi>=0.110
uvicorn[standard]>=0.27
openai>=1.30
pydantic>=2.5
python-dotenv>=1.0
sqlalchemy>=2.0
numpy>=1.24
# Stage 2 新增：SQLAlchemy 2.x ORM + SQLite 持久化
# Stage 2C 新增：numpy 加速向量 cosine 检索（1 万级以下纯 Python 也行，10 万级以上 numpy 必备）
# 后期切 PostgreSQL：再装 psycopg2-binary，把 DB_URL 改成 postgresql://... 即可
